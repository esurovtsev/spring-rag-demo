/**
 * This package contains all web-layer components of the application.
 * 
 * <p>The web package encapsulates all components related to the REST API, including
 * controllers, request/response DTOs, validation, and web-specific configuration.
 * This approach groups related web concerns together rather than splitting them
 * by technical function.</p>
 * 
 * <p>Key components:</p>
 * <ul>
 *   <li>RagController: Exposes the main /ask endpoint for RAG queries</li>
 *   <li>Request/Response DTOs: Define the API contract for web interactions</li>
 * </ul>
 */
package com.grabduck.springrag.web;

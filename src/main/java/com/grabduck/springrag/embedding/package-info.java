/**
 * This package contains components responsible for embedding generation.
 * 
 * <p>The embedding components convert text into vector representations using
 * OpenAI's embedding models. These embeddings are used for 
 * semantic search in the RAG workflow.</p>
 * 
 * <p>Key components:</p>
 * <ul>
 *   <li>EmbeddingClient: Handles communication with OpenAI API</li>
 *   <li>EmbeddingService: Business logic for embedding operations</li>
 *   <li>EmbeddingRequest/EmbeddingResponse: Data models for embedding operations</li>
 * </ul>
 */
package com.grabduck.springrag.embedding;

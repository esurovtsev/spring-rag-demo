# Stateless RAG App with Spring AI: Project Plan

## Notes
- Stateless RAG pattern: Retrieve (vector search) + Generate (LLM)
- No conversation history is stored; each query is processed independently
- Embeddings must be generated by the app (not the vector DB)
- Qdrant chosen for vector storage (open-source, Docker-friendly, Spring AI support)
- OpenAI `text-embedding-3-small` for embeddings, `gpt-4o-mini` for LLM
- REST API is the primary interface; optional UI via simple frontend
- Project will be initialized using Spring Initializr (in existing 'spring-rag-demo' folder, Java 21)
- Base package will be `com.grabduck.springrag`
- Project will be demoed on YouTube (GrabDuck) and used as a portfolio reference
- The architectural decision about logical organization of classes and concerns is now documented in the README.md
- The `config` package is a standard Spring Boot configuration package; its purpose is self-explanatory and not documented in detail in the README.md
- The domain package now uses a split-by-component-type structure: domain models are in `domain.model`, ports (interfaces) in `domain.port`, and orchestration services (like RagService) remain in the domain root. This follows best practices for separation of concerns and maintainability.
- Decision: The `Embedding` domain model must always include the source text (not just the vector). The codebase was just refactored to enforce this, ensuring all embedding objects are fully traceable and consistent.
- Introduce a `domain` package for the application core: contains rich domain models and orchestration services; all other packages (embedding, chat, vectorstore, web, config) act as adapters and depend on domain, but not on each other
- Use a domain-centric package structure: each functional package (embedding, chat, vectorstore, etc.) contains its own adapters directly, with no extra subpackage hierarchy for simplicity; the `domain` package is reserved for cross-cutting domain models and orchestration services only. As per user agreement and recent actions, all domain classes (models, ports, orchestration, etc.) should be placed directly in the `domain` package—do not use a `model` subpackage.
- RAG demonstration will use Amazon Return Policy content; a static JSON file will be included in the project and loaded into the vector store at startup if empty (pre-populated data approach)
- The API will include a feature toggle (request param) to enable/disable RAG (default: on) for comparison purposes
- The project will use an incremental, step-by-step branching approach: each major feature (embedding, chat, vectorstore, etc.) will be implemented and demonstrated in its own branch for clear review and demo progression.
- Always wait for explicit user approval before proceeding to the next implementation step, as repeatedly requested by the user.

## Task List
- [x] Initialize project using Spring Initializr (base package: com.grabduck.springrag, Java 21, in spring-rag-demo folder)
- [x] Create base project directory structure (if needed)
- [x] Create a general description of the project in README.md
- [x] Add key package structure (embedding, chat, vectorstore, web, config, domain) description to the README.md
- [x] Step 2: Project Configuration (branch step-2-project-configuration)
    - [x] Create empty base package structure (embedding, chat, vectorstore, web, config, domain)
    - [x] Add Spring Boot and relevant dependencies (Spring AI, OpenAI, Qdrant)
    - [x] Set up basic application properties (application.yml)
    - [x] Add rich domain models and ports to the domain package
        - [x] Define core domain models: Document (as record)
        - [x] Define core domain models: Query, Response
        - [x] Define core domain models: Embedding, etc.
        - [x] Define port interfaces: EmbeddingClient, VectorStore, ChatClient
- [x] Step 3: Embedding Implementation (branch step-3-embedding-implementation)
    - [x] Implement EmbeddingClient interface/class in embedding package
    - [x] Add configuration for OpenAI embedding model
    - [x] Write unit/integration tests for embedding functionality
    - [x] Add simple integration test to embed 'hello world' and print vector
- [ ] Step 4: Vector Store Integration (branch step-4-vector-store)
    - [ ] Add Docker Compose for Qdrant
    - [ ] Configure Qdrant connection in application properties
    - [ ] Implement Qdrant client/service in vectorstore package
    - [ ] Write tests for storing/retrieving vectors
- [ ] Step 5: Chat/LLM Integration (branch step-5-chat-integration)
    - [ ] Implement ChatClient interface/class in chat package
    - [ ] Add configuration for OpenAI LLM model
    - [ ] Write unit/integration tests for chat functionality
    - [ ] Add prompt templates for testing
- [ ] Step 6: VectorStore Abstraction (branch step-6-vectorstore-abstraction)
    - [ ] Implement VectorStore abstraction
    - [ ] Integrate with EmbeddingClient and Qdrant
    - [ ] Write tests for end-to-end vector storage/retrieval
- [ ] Step 7: RAG Service Implementation (branch step-7-rag-service)
    - [x] Implement RagService class in domain package
    - [ ] Wire together embedding, vectorstore, and chat components
    - [ ] Add business logic for RAG orchestration
    - [ ] Write tests for full RAG workflow
- [ ] Step 8: REST API and Feature Toggle (branch step-8-rest-api)
    - [ ] Implement controller class in web package
    - [ ] Add `/ask` endpoint with request/response DTOs
    - [ ] Integrate with RagService
    - [ ] Add feature toggle (request param) to enable/disable RAG (default: on)
    - [ ] Write API tests for endpoint
- [ ] Step 10: (Optional) Basic Frontend UI (branch step-10-frontend-ui)
    - [ ] Implement simple UI for testing `/ask` endpoint
    - [ ] Connect UI to backend API
- [ ] Step 11: Amazon Return Policy Data Initializer (branch step-11-data-initializer)
    - [ ] Create static JSON file with Amazon Return Policy content
    - [ ] Implement DataInitializer component
    - [ ] On startup, check if vector DB is empty and load data if needed
    - [ ] Test data initialization logic
- [ ] Step 12: Demo Walkthrough and Documentation (branch step-12-demo-docs)
    - [ ] Document each step/branch in README or separate doc
    - [ ] Add usage instructions and screenshots
    - [ ] Record or script demo for YouTube

## Current Goal
Step 4: Vector Store Integration (branch step-4-vector-store)